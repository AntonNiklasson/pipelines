{
  "private": true,
  "packageManager": "pnpm",
  "scripts": {
    "lint": "pnpm run --parallel lint",
    "test": "pnpm run test",
    "format:check": "pnpm run format:check"
  },
  "dependencies": {
    "eslint": "^8.33.0",
    "prettier": "^2.8.3",
    "vitest": "^0.28.3"
  },
  "workspaces": [
    "packages/ui",
    "packages/utils",
    "packages/app"
  ]
}
